# Attendance Tracker Setup Script

## Description
This script automates the setup of an attendance tracking project. It creates the required directory structure, validates user input, updates configuration values, checks the Python environment, and safely handles interruptions.

## How to Run
```bash
chmod +x attendance_setup.sh
./attendance_setup.sh

